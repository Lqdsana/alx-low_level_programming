#include <stdio.h>

/**
 * main-Entry point
 * prints the name of the program, including the path.
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[]);
	return (0);
}
